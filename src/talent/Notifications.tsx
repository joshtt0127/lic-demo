import { NotificationsScreen } from '@/features/notifications/NotificationsScreen'

/** Talent notifications — the shared screen. */
export function Notifications() {
  return <NotificationsScreen base="/talent" />
}
